Using System;

public class IfElse
  {
    static void Main(string[] args)
    {
      // program to demo the if else statement
      // variables 
      int a = 5, b = 5, c = 10;

      // if a is equal to b 
      if (a == b)
        // orint the var 
        Console.WriteLine(a);
// or statement 
      if ((a > c) || (a == b))
        Console.WriteLine(b);
       // and statement
      if ((a >= c) && (b <= c))
        // print statement 
        Console.WriteLine(c);
      // last statement 
    }
  }
// end of the program
}

public class Swap
{
   public static void Main()
   {
    // the initial variable
    int a = 5;
    // the second variable 
    int b = 3;
    // the swap variable
    int swap = 0;
    // print vriabls
    Console.WriteLine(a);
    Console.WriteLine(b);
      // swap the variables
      // set the intial variable to swap
    swap = a;
    a = b;
    // end the swap
    b = swap;
    // print the new variables
    Console.WriteLine(a);
    Console.WriteLine(b);
      // end of prigram
  }
}
